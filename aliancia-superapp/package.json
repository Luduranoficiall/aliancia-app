{
  "name": "aliancia-superapp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --filter=aliancia-app",
    "build": "turbo run build --filter=aliancia-app",
    "start": "turbo run start --filter=aliancia-app",
    "test": "turbo run test --filter=aliancia-app"
  },
  "packageManager": "npm@10.7.0",
  "workspaces": [
    "apps/aliancia-app",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "latest"
  }
}
